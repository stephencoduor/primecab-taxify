import { StyleSheet } from 'react-native'
import { windowHeight } from '../../../../theme/appConstant'

const styles = StyleSheet.create({
  container: { bottom: windowHeight(0.5) },
})
export default styles
